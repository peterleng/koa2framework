<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <script src="<%= config.res_host %>common/js/jquery-2.1.1.js"></script>
    <script src="<%= config.res_host %>common/bootstrap/js/bootstrap.min.js"></script>
    <link href="<%= config.res_host %>common/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <title><%= title %></title>
    <style>
        .w1200{width: 1200px;}
        .marginAuto{margin: 0 auto;}
    </style>
</head>
<body>
<div class="gray-bg marginAuto w1200">
<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">新闻系统</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li <% if(nav==='index'){ %>class="active" <% }%>>
                    <a href="/">首页 <span class="sr-only">(current)</span></a>
                </li>
                <li <% if(nav && nav==='news'){ %>class="active" <% }%>>
                    <a href="/news">新闻列表</a>
                </li>
                <li <% if(nav && nav==='chat'){ %>class="active" <% }%>>
                    <a href="/chatroom">聊天室</a>
                </li>
            </ul>

            <div class="nav navbar-nav navbar-right">
                <% if(user){ %>
                <span>您好，<%= user.nick %></span>
                <a href="/profile">个人中心</a>
                <a href="javascript:;" class="btn-logout">退出</a>
                <% }else{ %>
                <a href="/login">登录</a>
                <a href="/register">注册</a>
                <% } %>
            </div>
        </div>

    </div>
</nav>